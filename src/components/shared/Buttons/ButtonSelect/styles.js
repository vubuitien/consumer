import {
  StyleSheet,
} from 'react-native';

export const styles = StyleSheet.create({
  wrapper: {
    flexDirection: 'row',
    marginTop: 10,
  },
  textField: {
    color: '#525467',
    fontStyle: 'italic',
    fontSize: 16,
    lineHeight: 24,
  },
  inputField: {
    marginTop: -30,
  }
});
