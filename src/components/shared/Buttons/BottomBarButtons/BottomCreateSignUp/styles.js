import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  savebutton: {
    flexDirection: 'row',
  },
  name: {
    fontSize: 16,
    fontWeight: 'bold',
    fontStyle: 'italic',
    marginLeft: 3,
  },
  creat: {
    fontSize: 15,
    fontStyle: 'italic',
  }
});

export default styles;
