import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  name: {
    fontSize: 13,
    color: '#fff',
    fontStyle: 'italic',
  },
  creat: {
    fontSize: 14,
    color: '#fff',
    fontWeight: 'bold',
    fontStyle: 'italic',
    lineHeight: 14,
  },
  icon: {
    fontSize: 26,
    color: '#fff',
  }
});

export default styles;
