import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  textInput: {
    fontSize: 11,
    textAlign: 'center',
    color: '#525467',
    fontStyle: 'italic',
  }
});

export default styles;
