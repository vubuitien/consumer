import {
  StyleSheet,
} from 'react-native';

export const styles = StyleSheet.create({
  number: {
    fontWeight: 'bold',
    textAlign: 'center',
  },
  status: {
    textAlign: 'center',
  }
});
