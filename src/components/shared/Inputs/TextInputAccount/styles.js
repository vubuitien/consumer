import {
  StyleSheet,
} from 'react-native';

export const styles = StyleSheet.create({
  wrapper: {
    flexDirection: 'row',
    width: 320,
    height: 45,
    backgroundColor: '#fff',
    marginTop: 20,
  },
  text: {
    width: 320,
    fontSize: 14,
  },
});
