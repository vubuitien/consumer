import {
  StyleSheet,
} from 'react-native';

export const styles = StyleSheet.create({
  wrapper: {
    width: '100%'
  },
  text: {
    fontSize: 14,
    textAlign: 'center',
    color: '#525467',
    fontStyle: 'italic',
  },
});
