import {
  StyleSheet,
} from 'react-native';

export const styles = StyleSheet.create({
  checkbox: {
    borderColor: '#787878',
    borderWidth: 2,
  },
  row: {
    flexDirection: 'row',
    marginTop: 10,
  },
  textName: {
    fontSize: 15,
    color: '#000',
    marginLeft: 20
  }
});
