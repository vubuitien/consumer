import {
  StyleSheet,
} from 'react-native';
import Fonts from '../../../../../../constants/fonts';

export const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    marginRight: 10,
  },
  styleIcon: {
    color: '#fff',
    fontSize: 16,
  },
  category: {
    color: '#fff',
    fontSize: 16,
    fontFamily: Fonts.ubuntuItalic,

  }
});
