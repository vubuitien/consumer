import { StackNavigator } from 'react-navigation';
import RouteConfigs from './route_configs';
import StackNavigatorConfig from './stack_navigator_config';

export default AccountNavigator = StackNavigator(RouteConfigs, StackNavigatorConfig);
